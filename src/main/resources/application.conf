akka {
  loglevel = DEBUG
  loglevel = ${?loglevel}
  log-config-on-start = "on"
  log-config-on-start = ${?log-config-on-start}
}

http {
  interface = "0.0.0.0"
  interface =${?interface}
  port = 9000
  port = ${?port}
}


akka.http.server {
  request-timeout = 60s
  request-timeout = ${?request-timeout}
}


databricks {

 warehouse {
  jdbc = "jdbc:databricks://dbc-61ea0a52-b926.cloud.databricks.com:443/default;transportMode=http;ssl=1;AuthMech=3;httpPath=/sql/1.0/warehouses/29d68b0c32c62b03"
  jdbc = ${?jdbc}
  token = ""
  token = ${?token}
 }

 data {
  catalog = "databricks_xpo_fhir_delta_share"
  catalog = ${?catalog}
  schema = "databricks_fhir_service_forked"
  schema = ${?schema}
 }	
}


databricks.pool {
  minIdle = 2
  maxPoolSize = 10
}
